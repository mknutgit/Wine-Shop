{{#if addNewProduct}}
<h1>New Product</h1>
    <div>
      <form>
        <div class="form-group">
          <label for="producer">Producer</label>
          {{input value=producer id="producer"}}
        </div>

        <div class="form-group">
          <label for="varietal">Varietal</label>
          {{input value=varietal id="varietal"}}
        </div>

        <div class="form-group">
          <label for="image">Image URL</label>
          {{input value=image id="image"}}
        </div>

        <div class="form-group">
          <label for="description">Description of Product</label>
          {{textarea value=description id="description" rows="5"}}
        </div>

        <div class="form-group">
          <label for="cost">Cost of Product</label>
          {{input type="number" value=cost id="cost"}}
        </div>

        <button class="btn-lg btn-success"{{action 'addProduct'}}>Save</button>
      </form>
    </div>
{{else}}
  <button class="btn-lg btn-primary"{{action 'productFormShow'}}>Add New Product</button>
{{/if}}
